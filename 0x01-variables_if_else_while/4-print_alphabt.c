#include <stdio.h>
/**
 * main - main function
 * x not c variable description
 * use `putchar` to print all letters but the letter 'q' and 'e'.
 * Return: 0 (Success)
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		if (c != 'q' && x != 'e')
			putchar(x);
		x++;
	}
	putchar('\n');

	return (0);
}
